/*
//顺序表静态存储方式
//假定线性表的元素类型为: ElemType

#define MaxSize 50  //定义线性表的最大长度，假设为50

typedef struct{
    ElemType elem[MaxSize]; //用一维数组来存储顺序表的元素
    int length;     //顺序表的当前长度
  }Sqlist;      //顺序表的类型定义

//以上的顺序表的类型定义后，可以用语句“Sqlist L;” 定义一个顺序表L

*/

